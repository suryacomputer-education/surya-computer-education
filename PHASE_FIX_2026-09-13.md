# SURYA CIMP — Phase Fix 2026-09-13

## Fixed
- Course Management money fields now accept normal numeric/currency text safely and optional discount/admission fee can remain blank.
- Course validation now uses normalized values and checks admin session before sending the request.
- Courses setup migration remains centralized in `SetupSheets.gs`.
- Public Media now generates canonical Google Drive view + thumbnail URLs from File ID.
- Public Media upload verifies that the Drive file is actually shared as `ANYONE_WITH_LINK`; inaccessible uploads are rejected instead of silently creating blank gallery cards.
- Admin Media preview uses thumbnail/view fallback.
- Gallery now includes all available static institute images, including `IMG-20260822-WA0001.jpg`.
- Gallery viewer now supports Previous/Next, keyboard arrows, Escape, and mobile left/right swipe.
- Gallery image loading has a Drive thumbnail fallback.
- Google SEO metadata improved: canonical URLs, descriptions, keywords, Open Graph image and site metadata on public pages.
- `index.html` now contains `EducationalOrganization` JSON-LD entity data for SURYA COMPUTER OF EDUCATION CENTER.
- `sitemap.xml` now uses the official GitHub Pages URL.
- Added `robots.txt` pointing to the official sitemap.
- Removed remaining old the previous GitHub Pages host references from the project.
- Fixed a syntax error in `assets/js/admin-certificates.js`.

## Important backend action
Because `Media.gs` changed, push the backend with `clasp push` and redeploy the existing Web App deployment after testing.

## SEO note
Meta keywords are included for documentation/legacy compatibility, but Google ranking is primarily driven by useful page content, titles/descriptions, crawlability, structured data, links and local business/entity signals.
